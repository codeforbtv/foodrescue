<h1>Results</h1>

<p>Thank for your inquiry, creating partnerships with local food distribution centers is a great way to help your community.</p>
<p>Based on your answer, we recommend contacting the following resources for donation of your food.</p>

<div class="results">
  <section class="food-shelves">
    <h3><i></i>Food Shelves</h3>
    <% if @survey_response.edible? %>
        <% @foodshelf.each do |result| %>
          <%= render 'result', result: result %>
        <% end %>
    <% else %>
        <li>This food is not elligible to donate to a food shelf. Please see the other resources below</li>
    <% end %>
  </section>

  <section class="farms">
    <h3><i></i>Pig Farms</h3>
    <% @pig.each do |result| %>
      <%= render 'result', result: result %>
    <% end %>
  </section>

  <section class="compost">
    <h3><i></i>Composting Facilities</h3>
    <% @compost.each do |result| %>
      <%= render 'result', result: result %>
    <% end %>
  </section>
</div>
